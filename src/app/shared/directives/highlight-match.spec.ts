import { HighlightMatch } from './highlight-match';

describe('HighlightMatch', () => {
  it('should create an instance', () => {
    const directive = new HighlightMatch();
    expect(directive).toBeTruthy();
  });
});
